#nojrs
name: manual working

on:
  push:
    branch: [master]
    

  workflow_dispatch:
    inputs:
      name:
        description: 'name for the yml'
        required: true
        type: string
      greeting:
        description: 'type the greeting'
        required: true
        type: string
      date:
        description: 'date'
        required: true
        type: true




jobs:
  hello: 
    runs-on: ubuntu-latest
    steps:
      - name: helloing
        run: echo "hi , ${{inputs.name}}"
      - name: greeting
        run: echo "${{inputs.greetig}} , ${{inputs.name}}, ${{inputs.date}}, trying to run this code with manual approach help meeeeeee"


    